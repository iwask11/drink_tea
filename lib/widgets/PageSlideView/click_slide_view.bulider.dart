import 'package:flutter/material.dart';

///可以点击又可以滑动的pageview轮播组件。带有小圆点或者被不带小圆点
///带小圆点的：传入图片view宽高，
///不带小圆点的：传入每一页的item的宽高计算后得出的view宽高
///传入值：bool isDot；viewHight；viewWidth；
///controller：控制器
///写成两个好啦，一个菜单一个轮播图的，本来就不是一起的。
class ClickSlideViewBuilder extends StatelessWidget {
  const ClickSlideViewBuilder({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
  
  
}
